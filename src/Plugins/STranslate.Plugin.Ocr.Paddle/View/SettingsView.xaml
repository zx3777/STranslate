<UserControl
    x:Class="STranslate.Plugin.Ocr.Paddle.View.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:vm="clr-namespace:STranslate.Plugin.Ocr.Paddle.ViewModel"
    d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <ikw:SimpleStackPanel Spacing="12">
        <!--  模型位置配置  -->
        <ui:SettingsExpander Description="{DynamicResource STranslate_Plugin_Ocr_Paddle_ModelsDirectory_Description}" Header="{DynamicResource STranslate_Plugin_Ocr_Paddle_ModelsDirectory}">
            <ui:SettingsExpander.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Location_20_Regular}" />
            </ui:SettingsExpander.HeaderIcon>
            <Button Command="{Binding SelectFolderCommand}" Style="{StaticResource AccentButtonStyle}">
                <ui:IconAndText Content="选择" Icon="{x:Static ui:FluentSystemIcons.Folder_20_Regular}" />
            </Button>

            <ui:SettingsExpander.Items>
                <ui:SettingsCard>
                    <ui:SettingsCard.Header>
                        <TextBox
                            MinWidth="300"
                            ui:ControlHelper.PlaceholderText="点击上方按钮选择"
                            IsReadOnly="True"
                            Text="{Binding ModelsDirectory}"
                            TextWrapping="Wrap" />
                    </ui:SettingsCard.Header>
                </ui:SettingsCard>
            </ui:SettingsExpander.Items>
        </ui:SettingsExpander>
    </ikw:SimpleStackPanel>
</UserControl>